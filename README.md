O projeto usa um ambiente venv e foi desenvolvido com python 3.12.10. Versões posteriores não vão funcionar. O projeto foi desenvolvido no VSCode.

python -m venv venv

pip install opencv-python opencv-contrib-python mediapipe numpy Pillow
